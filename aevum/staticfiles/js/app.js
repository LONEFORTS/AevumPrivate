function startTimer(){const input=document.querySelector('#id_minutes');const display=document.getElementById('timerDisplay');if(!input||!display)return;let seconds=(parseInt(input.value||25,10))*60;display.textContent=formatSeconds(seconds);const timer=setInterval(()=>{seconds--;display.textContent=formatSeconds(seconds);if(seconds<=0){clearInterval(timer);display.textContent='Done';alert('Focus session finished. Tap Save Completed Session.');}},1000);}function formatSeconds(total){const m=String(Math.floor(total/60)).padStart(2,'0');const s=String(total%60).padStart(2,'0');return `${m}:${s}`;}document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('input, textarea, select').forEach(el=>el.classList.add(el.tagName==='SELECT'?'form-select':'form-control'));});